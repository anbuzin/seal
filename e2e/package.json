{
  "name": "e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Full-stack end-to-end tests: drive the running seal app (frontend + backend + worker) via a real browser.",
  "type": "module",
  "scripts": {
    "test": "node test.mjs",
    "test:images": "node images.mjs",
    "install-browser": "playwright install chromium"
  },
  "devDependencies": {
    "playwright": "1.61.0"
  }
}
